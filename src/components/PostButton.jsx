import '../styling/PostButton.css'; // This imports the CSS from a separate file

export const PostButton = () => {
    return (
        <button className="post-button">
            Post
        </button>
    );
};
